const CartReducer = (state, action) => {
    switch(action.type){
        case "Add":

        case "Remove":

        case "Increase":

        case "Decrease":

        default:
            state;
    }
}

export default CartReducer